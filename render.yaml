services:
  # ─── BACKEND ────────────────────────────────────────────────
  - type: web
    name: franchise-iq-backend
    env: docker
    region: ohio
    plan: free
    dockerContext: .
    dockerfilePath: ./backend/Dockerfile
    healthCheckPath: /health

    envVars:
      - key: CORS_ORIGINS
        value: "https://franchise-iq-frontend.onrender.com"
      - key: DATA_DIR
        value: "/app/data"
      - key: AMENITIES_DIR
        value: "/app/amenities_cache"
      - key: UPLOADS_DIR
        value: "/app/uploads"
      - key: OUTPUTS_DIR
        value: "/app/outputs"
      - key: LOGS_DIR
        value: "/app/logs"
      - key: DOWNLOADS_DIR
        value: "/app/downloads"
      - key: REAL_ESTATE_DIR
        value: "/app/real_estate_data"

  # ─── FRONTEND ───────────────────────────────────────────────
  - type: web
    name: franchise-iq-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist

    envVars:
      - key: VITE_API_URL
        value: "https://franchise-iq-backend.onrender.com"
      - key: VITE_API_URL_DYNAMIC
        fromService:
          name: franchise-iq-backend
          type: web
          property: hostWithScheme   # ✅ FIXED

    routes:
      - type: rewrite
        source: /*
        destination: /index.html